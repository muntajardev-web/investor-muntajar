[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  # Do NOT set NODE_ENV here — Next sets it during build. Forcing it breaks /_global-error.
